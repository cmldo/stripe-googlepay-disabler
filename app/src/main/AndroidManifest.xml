<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:label="@string/app_name"
        android:description="@string/app_description"
        android:allowBackup="false"
        android:hasCode="true">

        <!--
            LSPosed / Xposed module declarations.
            xposedminversion 93 = LSPosed 1.8+
        -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="Forces Stripe CustomerSheet googlePayEnabled() to always be false" />
        <meta-data
            android:name="xposedminversion"
            android:value="93" />

        <!--
            Recommended scope hint shown in LSPosed Manager.
            Users still pick the exact target app(s) in the UI.
        -->
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />

    </application>

</manifest>
